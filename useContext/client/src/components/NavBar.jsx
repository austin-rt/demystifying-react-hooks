import ThemeLabel from './ThemeLabel';
import ThemeSliderToggle from './ThemeToggle';

const NavBar = () => {
  return (
    <nav>
      <ThemeSliderToggle />
      <ThemeLabel />
    </nav>
  );
};
export default NavBar;
